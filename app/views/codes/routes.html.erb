<pre>
<h1>Console - rake routes</h1>
In the config/routes.rb file add 

<p><b>resources :core_values</b></p>
<p>and get the routes below:</p>

<pre style="color:red;">routes path     HTTP verb link                         controller#action

core_values     GET    /core_values(.:format)          core_values#index
                POST   /core_values(.:format)          core_values#create
 new_core_value GET    /core_values/new(.:format)      core_values#new
edit_core_value GET    /core_values/:id/edit(.:format) core_values#edit
     core_value GET    /core_values/:id(.:format)      core_values#show
                PATCH  /core_values/:id(.:format)      core_values#update
                PUT    /core_values/:id(.:format)      core_values#update
                DELETE /core_values/:id(.:format)      core_values#destroy
</pre>                 
<p>To view the routes above in the console add</p>
<p><b>rake routes</b></p>
             
<h1>Console - rake routes</h1>
In the config/routes.rb file add
                
resources :articles 

     articles   GET    /articles(.:format)             articles#index
                POST   /articles(.:format)             articles#create
  new_article   GET    /articles/new(.:format)         articles#new
 edit_article   GET    /articles/:id/edit(.:format)    articles#edit
      article   GET    /articles/:id(.:format)         articles#show
                PATCH  /articles/:id(.:format)         articles#update
                PUT    /articles/:id(.:format)         articles#update
                DELETE /articles/:id(.:format)         articles#destroy
                
</pre>

<h1>routes.rb</h1>
<p>resources :user_stocks, except: [:show, :edit, :update]</p>
<p>This will create routes for user_stocks except show, edit and update</p>